#cloner le projet 

#lancer la cmd: npm install

#cree un projet react ailleur et copier le dossier
 node_modules vers le dossier summaryfront du projet cloné dans 
 par la cmd npx create-react-app summarufront

dnas le meme chemin installer axios^par " npm i axios "



#lancer mongo db (apres installation ) dans un chemin où existe le dossier data
mongod --dbpath=data --bind_ip 127.0.0.1

#cree la base de donnée 
dans un autre cmd : 
1 mongo
2 use resumApp 

#lancer le serveur par npm start dans le chemin du projet global 
#lancer le client par npm start dans summaryfront